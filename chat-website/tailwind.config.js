module.exports = {
  theme: {
    extend: {
      colors: {
        primary: {
          dark: '#111b21',    // Dark background
          DEFAULT: '#202c33', // Sidebar/header background
          light: '#2a3942',   // Input background
        },
        accent: {
          DEFAULT: '#00a884', // Primary accent color (buttons)
          hover: '#008f6f',   // Hover state
        },
        text: {
          primary: '#e9edef', // Primary text
          secondary: '#8696a0', // Secondary text
          muted: '#667781',   // Muted text
        }
      }
    }
  }
}